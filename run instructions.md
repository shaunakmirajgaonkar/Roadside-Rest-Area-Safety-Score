# Run Instructions

```bash
cd ~/Downloads
unzip -o Roadside_Rest_Area_Safety_Score_LATEST_ONLY.zip
cd Roadside_Rest_Area_Safety_Score

python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
streamlit run app.py
```

Do not type the `%` or `$` terminal prompt shown before commands.
